import { SectionHeader, Video } from "../containers";
import { Main } from "../layouts";

const index = () => {
  return (
    <Main>
      <Video />
      <SectionHeader />
    </Main>
  );
};

export default index;
